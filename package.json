{
  "name": "pharma-blog-auto-poster-frontend",
  "version": "1.0.0",
  "description": "PharmaBlog Auto Poster - Frontend Only",
  "type": "module",
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "cd client && npm install && npm run build",
    "start": "cd client && npm run preview",
    "install": "cd client && npm install"
  },
  "dependencies": {},
  "devDependencies": {}
}